# Roda lint e formatação antes de cada commit. Instale uma vez com: pre-commit install
# É o mesmo ruff que roda no CI (ver .github/workflows/ci.yml).
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.9
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: end-of-file-fixer
      - id: trailing-whitespace
        exclude: \.md$
      - id: check-yaml
      - id: check-json
      - id: check-added-large-files
        args: [--maxkb=500]
